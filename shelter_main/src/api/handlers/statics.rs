// use axum::{ServiceExt, Router};
// use tower_http::services;
// use std::sync::Arc;

// use crate::{api::response::error::AppError, state::ApplicationState};

pub struct Dsssd {}
// // pub async fn serve( state: Arc<ApplicationState>)-> Router {
// //     let path = "./static";
// //     services::ServeDir::new(path).into_make_service().
// // }
